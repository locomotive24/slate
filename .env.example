PORT=4000
JWT_SECRET=replace-with-a-long-random-string
# The ORIGIN (scheme + host, no path!) of your GitHub Pages site.
# Multiple origins can be comma-separated. localhost is always allowed.
CLIENT_ORIGIN=http://localhost:5500
